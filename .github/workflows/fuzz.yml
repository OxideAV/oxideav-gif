name: Fuzz

# Daily fuzz run for the cargo-fuzz harnesses under fuzz/. Splits the
# 30-minute total budget across the 3 targets (~10 min each). The
# cross-decode harnesses dlopen giflib at runtime via libloading; the
# CI runner needs `libgif-dev` so they actually validate instead of
# silently skipping.

on:
  schedule:
    - cron: "31 5 * * *"   # daily 05:31 UTC, jittered off the hour
  workflow_dispatch:

jobs:
  fuzz:
    uses: OxideAV/.github/.github/workflows/crate-fuzz.yml@master
    with:
      extra_packages_apt: "libgif-dev"
      time_budget_seconds: 1800
    secrets: inherit
